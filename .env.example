# Copy to .env.local (never commit secrets). Single-page Tirith reads sync URL + secret from index.html config (C.supabaseSyncUrl / C.syncSecret), not from env.

# Supabase CLI / Edge Function deploy
# SUPABASE_ACCESS_TOKEN=
# SYNC_SECRET=generate-a-long-random-string
